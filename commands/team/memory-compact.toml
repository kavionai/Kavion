description = "Compact ForgeKit 2 memory safely."

prompt = """
Compact ForgeKit 2 memory safely.

Required:
1. Keep `.gemini/forgekit/CURRENT.md` focused on the active task only.
2. Delete expired notes under `.gemini/forgekit/notes/` unless marked persistent.
3. Report oversized PROJECT.md, CURRENT.md, or DECISIONS.md.
4. Refresh `.gemini/forgekit/index/` after compaction if indexing is in use.

Use MCP tool `forgekit_memory_gc` when available.
Do not delete useful live session state.
"""
