description = "Resume the current ForgeKit task from saved project memory."

prompt = """
Resume the current ForgeKit task.

Required:
1. Read `.gemini/forgekit/session.json`.
2. Read `.gemini/forgekit/CURRENT.md`.
3. Read `.gemini/forgekit/PROJECT.md` or `.gemini/forgekit/DECISIONS.md` only if needed.
4. Continue from the saved next step.

Do not restart planning from scratch if a valid session and plan already exist.
"""
