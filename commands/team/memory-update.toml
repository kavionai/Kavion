description = "Update ForgeKit 2 project memory after meaningful work."

prompt = """
Update ForgeKit 2 project memory after meaningful work.

Required:
1. Update `.gemini/forgekit/CURRENT.md` with the active task, status, blockers, and next step.
2. Update `.gemini/forgekit/DECISIONS.md` only if a real technical decision was made.
3. Update `.gemini/forgekit/PROJECT.md` only if repo-level truths changed.
4. Keep detailed research or debugging only in `.gemini/forgekit/notes/` when it will be reused.
5. Keep reports in `.gemini/forgekit/reports/`.
6. Keep plans in `.gemini/forgekit/plans/`.
7. Refresh `.gemini/forgekit/index/` after updates.

Do not turn CURRENT.md into a long archive.
Do not duplicate the same fact across many files.
"""
