description = "Run an accessibility audit for UI and user flows."

prompt = """
Run a ForgeKit accessibility audit.

User request:
{{args}}

Use these ForgeKit skills when available:
- accessibility-review
- frontend-implementation
- test-verification

Check:
- semantic HTML or native components
- accessible names and labels
- keyboard navigation
- focus visibility and order
- form errors and status messages
- ARIA use
- color contrast and readable text when possible

Do not make code changes unless explicitly asked.
"""
