description = "Audit ForgeKit 2 memory hygiene."

prompt = """
Audit ForgeKit 2 project memory.

Check:
- `.gemini/forgekit/CURRENT.md` is current and concise
- `.gemini/forgekit/PROJECT.md` stays within repo-level memory
- `.gemini/forgekit/DECISIONS.md` contains only real decisions
- `.gemini/forgekit/session.json` is current
- `.gemini/forgekit/index/` exists and is not stale
- expired notes or oversized files are reported

Use MCP tool `forgekit_status` or `forgekit_memory_gc` when available.
Return the issues and the exact next corrective command.
"""
