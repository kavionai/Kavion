description = "Audit the current ForgeKit workflow without doing implementation work."

prompt = """
Audit the current ForgeKit workflow.

Use MCP tool `forgekit_status` when available, and use `forgekit_gate` if a gate result is needed.

Check:
- session state
- current work
- plan presence
- report freshness
- index state
- expired notes or oversized memory files

Return the gaps and the next corrective command only.
"""
