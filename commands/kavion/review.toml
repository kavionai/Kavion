description = "Review current code changes using the Kavion review workflow."

prompt = """
Review the current code changes using the Kavion review workflow.

Use these Kavion skills when available:
- code-review-workflow
- security-review
- test-verification

Required flow:
1. Use code-reviewer to review the current diff for bugs, regressions, maintainability, and missing tests.
2. Use security-engineer if changes touch auth, permissions, secrets, payments, user data, or external input.
3. Use qa-test-engineer to recommend or run relevant tests if needed.

Focus on real issues, not style preferences.
Return findings ordered by severity.
If no issues are found, say that clearly and mention any remaining test gaps.
"""
